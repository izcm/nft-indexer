export async function runNFTBackfillWorker() {
  // the nft worker needs to check whether a new collection exists that has
  // use chainid and get collections on that chainid that has pending backfill
  // foreach do batch
}
